services:
  mcp-unraid:
    build:
      context: ../../
      dockerfile: servers/mcp-unraid/Dockerfile
    container_name: mcp-unraid
    restart: unless-stopped
    ports:
      - "3003:3000"
    networks:
      - mcp-net
    environment:
      - LOG_FILE=/logs/server.log
      - MCP_CORS_ORIGINS=*
      - MCP_PROTOCOL_VERSION=2024-11-05
    volumes:
      - ../../logs/mcp-unraid:/logs
      - ~/.ssh:/home/proxyuser/.ssh:ro
    healthcheck:
      test: ["CMD", "sh", "-c", "curl -s -X POST http://localhost:3000/mcp -H 'Content-Type: application/json' -H 'Accept: application/json, text/event-stream' -d '{\"jsonrpc\":\"2.0\",\"method\":\"initialize\",\"params\":{\"protocolVersion\":\"'$$MCP_PROTOCOL_VERSION'\",\"capabilities\":{},\"clientInfo\":{\"name\":\"healthcheck\",\"version\":\"1.0\"}},\"id\":1}' | grep -q '\"protocolVersion\":\"'$$MCP_PROTOCOL_VERSION'\"'"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 40s

networks:
  mcp-net:
    name: ${MCP_NETWORK:-mcp-oauth}
    external: true
